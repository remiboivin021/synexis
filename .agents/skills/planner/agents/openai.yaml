interface:
  display_name: "Planner"
  short_description: "Build scoped, testable engineering plans."
  default_prompt: "Use $planner to turn this request into a minimal execution plan with risks, gates, and acceptance criteria."
